import type { SupabaseClient } from '@supabase/supabase-js'

/**
 * 种子数据函数 - 已禁用
 * 不再自动插入任何测试数据
 */
export async function seedDatabaseIfEmpty(_supabase: SupabaseClient): Promise<void> {
  // 函数已禁用，不再插入任何数据
  // 保持函数签名以避免破坏现有代码
}
