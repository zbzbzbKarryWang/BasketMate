# BasketMate 快速测试指南

## 环境准备

### 1. 配置文件

后端需要配置环境变量：

```bash
# 进入后端目录，复制模板
cd backend
copy .env.example .env

# 编辑 .env 填入实际值
# - SUPABASE_URL
# - SUPABASE_KEY
# - BAIDU_OCR_API_KEY
# - BAIDU_OCR_SECRET_KEY
# - LLM_API_KEY
# - BOCHAAI_API_KEY
```

前端无需额外配置（本地开发使用 `.env.local`，已内置配置）。

---

## 方式一：浏览器测试（最简单！）

### 步骤：
1. 确保后端正在运行 (`uvicorn app.main:app --reload`)
2. 打开浏览器访问 `http://localhost:3000`
3. 按 `F12` 打开开发者工具
4. 切换到 **Console** 标签
5. 打开文件 `frontend/browser-test.js`，复制全部内容
6. 粘贴到控制台，按回车
7. 10秒后就能看到完整的测试结果！

---

## 方式二：Python API 测试

### 步骤：
1. 确保后端正在运行
2. 在终端运行：
   ```bash
   python test/test_unified_api.py
   ```

---

## 方式三：手动快速测试（5分钟）

### 最简测试清单：
1. ✅ 打开 http://localhost:3000
2. ✅ 看首页是否正常加载
3. ✅ 点击"库存"看食材列表
4. ✅ 点击"菜谱"看菜谱列表
5. ✅ 点击"计划"看计划列表
6. ✅ 按 F12 看 Network 标签，随便点几个功能
7. ✅ 检查所有 API 响应格式：
   ```json
   {
     "success": true,
     "message": "操作成功",
     "data": { ... }
   }
   ```

---

## Docker 部署测试

```bash
# 构建并启动所有服务
docker-compose up -d

# 查看日志
docker-compose logs -f

# 测试后端
curl http://localhost:8000/api/health

# 测试前端
curl http://localhost:3000
```

---

## 测试成功标准

### API 格式要求：
- 所有响应都必须有 `success`, `message`, `data` 三个字段
- 200 状态码时，`success` 必须是 `true`
- 4xx/5xx 状态码时，`success` 必须是 `false`

### 前端功能要求：
- 页面能正常加载
- 数据能正常显示
- 操作（添加、编辑、删除）能正常工作
- 错误提示友好

---

## 提示

1. **总是先测试后端**：测试前确保后端服务正在运行
2. **查看日志**：如果有问题，查看后端的日志输出
3. **Network 标签**：浏览器开发者工具的 Network 标签是最好的调试工具
4. **Swagger UI**：访问 http://localhost:8000/docs 可以直接在浏览器测试 API

---

## 最快的方式

**在浏览器控制台运行 `frontend/browser-test.js` 的内容！**
