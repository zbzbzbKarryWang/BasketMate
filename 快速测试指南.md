
# 🚀 BasketMate 快速测试指南

## 方式一：浏览器测试（最简单！） ⭐️⭐️⭐️

### 步骤：
1. 确保后端正在运行 (`uvicorn app.main:app --reload`)
2. 打开浏览器访问 `http://127.0.0.1:8000/docs` 或者 `http://localhost:3000`
3. 按 `F12` 打开开发者工具
4. 切换到 **Console** 标签
5. 打开文件 `frontend/browser-test.js`，复制全部内容
6. 粘贴到控制台，按回车
7. 10秒后就能看到完整的测试结果！

---

## 方式二：Python API 测试

### 步骤：
1. 确保后端正在运行
2. 在终端运行：
   ```bash
   python test/test_unified_api.py
   ```

---

## 方式三：Node.js API 测试（如果有 Node.js）

### 步骤：
1. 确保后端正在运行
2. 进入 frontend 目录：
   ```bash
   cd frontend
   ```
3. （可选）安装 undici（如果需要）：
   ```bash
   npm install undici --save-dev
   ```
4. 运行测试：
   ```bash
   node test-api.js
   ```

---

## 方式四：手动快速测试（5分钟）

### 最简测试清单：
1. ✅ 打开 http://localhost:3000
2. ✅ 看首页是否正常加载
3. ✅ 点击"库存"看食材列表
4. ✅ 点击"菜谱"看菜谱列表  
5. ✅ 点击"计划"看计划列表
6. ✅ 按 F12 看 Network 标签，随便点几个功能
7. ✅ 检查所有 API 响应格式：
   ```json
   {
     "success": true,
     "message": "操作成功",
     "data": { ... }
   }
   ```

---

## 📊 测试成功标准

### API 格式要求：
- 所有响应都必须有 `success`, `message`, `data` 三个字段
- 200 状态码时，`success` 必须是 `true`
- 4xx/5xx 状态码时，`success` 必须是 `false`

### 前端功能要求：
- 页面能正常加载
- 数据能正常显示
- 操作（添加、编辑、删除）能正常工作
- 错误提示友好

---

## 💡 提示

1. **总是先测试后端**：测试前确保后端服务正在运行
2. **查看日志**：如果有问题，查看后端的日志输出
3. **Network 标签**：浏览器开发者工具的 Network 标签是最好的调试工具
4. **Swagger UI**：访问 http://127.0.0.1:8000/docs 可以直接在浏览器测试 API

---

## 🎯 一句话总结

**最快的方式**：在浏览器控制台运行 `browser-test.js` 的内容！

